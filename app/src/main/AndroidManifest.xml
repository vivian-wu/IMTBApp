<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="imtbteam.imtb">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- android:roundIcon="@mipmap/ic_launcher_round" -->
        <activity
            android:name=".activity_start_menu"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".QRScannerActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FinanceActivity"
            android:label="@string/title_activity_finance"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".game_rule"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AccountingActivity" />
        <activity android:name=".JobScannerActivity" />
        <activity
            android:name=".assets_record"
            android:label="@string/title_activity_assets_record"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".about_us"
            android:label="@string/title_activity_about_us"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".temp"></activity>
    </application>

</manifest>